import 'package:flutter/material.dart';

class MeinerApp extends StatelessWidget {
  const MeinerApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}